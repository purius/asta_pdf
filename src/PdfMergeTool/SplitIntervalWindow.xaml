<Window x:Class="PdfMergeTool.SplitIntervalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="N페이지마다 분리"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="몇 페이지마다 새 PDF로 분리할까요?"
                   FontSize="15" />
        <TextBox x:Name="IntervalTextBox"
                 Grid.Row="1"
                 Margin="0,10,0,0"
                 MinWidth="220"
                 Text="1"
                 VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="취소"
                    MinWidth="76"
                    Margin="0,0,8,0"
                    IsCancel="True" />
            <Button Content="분리"
                    MinWidth="76"
                    IsDefault="True"
                    Click="OnSplitClick" />
        </StackPanel>
    </Grid>
</Window>
